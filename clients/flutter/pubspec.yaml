name: astr_auto_anima_hub_client
description: AstrAutoAnima Hub client for Windows and Android
publish_to: "none"
version: 0.3.8-dev.3+14

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.2
  flutter_secure_storage: ^9.2.2
  flutter_local_notifications: ^22.2.0
  file_picker: ^10.3.10
  path_provider: ^2.1.5
  public_file_saver: ^1.1.0
  saf: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
