# 0.5.0-beta.1 公开测试版

从最新内部主线制作的独立公共发布版本，版本号不与私人部署的 0.5.1-beta.4 混用。

## 包含

- AstrBot 插件、Hub 服务、Windows / Android / Web 客户端源码与对应发布构建。
- 最新脱敏工作流和自定义节点：Quick、HQ、Refine、SeedVR2、局部修复、多角色、复选反推、安全审核；独立 caption 辅助节点保留。
- 动态 LoRA、角色 / 画风预设、有序任务套组、光照 / 材质 / 镜头选项、随机词库分组、图片记录 / 下载、QQ 引用操作。
- 自定义词库编辑器与令牌生成 / 管理工具。词库内容为零，导入由用户负责审核与授权。
- 新部署向导：可选外部环境下载、依赖安装、配置与本机随机令牌、服务启动、就绪检查、可重复运行的 Start 入口。
- Hub 不再在导入时创建审核数据库，默认路径不依赖 `/workspace` 权限；向导按用户选择生成绝对路径，手动部署须设置实际路径。

## 明确不包含

- LoRA Studio / 炼丹炉连接器、Hub 扩展代理 / 会话接口、App 炼丹炉入口。
- 私人服务器启动 / 强制重启脚本和依赖 Studio 台账的自动云关机功能；避免公共环境误关机。
- 任何生产 API Key、Hub Token、QQ 登录态、私人角色词库、随机提示词正文、LoRA / 模型权重、私人 OC 开屏或主题插画。
- 作者实际工作流配置；模型名与 LoRA 名使用占位符，或由部署向导根据用户自己选择的模型在本机生成。

## 升级注意

先备份配置、插件数据、用户词库和工作流。不要直接用旧完整目录覆盖新公开版，否则可能把私人文件重新带入。
旧客户端可并行使用但不保证支持最新字段；推荐升级配套 App。Android APK 为社区测试签名，不是商店正式签名；签名不匹配时请先导出设置再换装。

Anima Master 已联调版本仍为 **0.7.2**；**0.8.0 未测试**。高级工作流和全新系统部署属于 Beta，不保证所有显卡与第三方节点组合。安装失败不会自动强杀进程或删除用户模型。

## 验证边界

发布前执行隐私扫描、结构 / 语法检查、插件 / Hub / 工具测试、客户端静态分析与构建。私人词库统计与迁移快照测试不随公共数据分发；公开行为以合成样例测试。
这些检查不替代真实 GPU 生图、QQ 登录、各厂商云实例或所有全新操作系统实测。详见发布附件中的验证报告。
