# 安全说明

## 报告漏洞

请通过 GitHub Security Advisory 私下报告可复现的鉴权绕过、任意文件读写、令牌泄漏或远程
任务越权问题。报告中不要附带真实 QQ 登录数据、生产令牌或私有图片。

## 部署基线

- Hub 默认监听 `127.0.0.1`；不要把 6278 直接裸露到公网。
- 远程访问使用 HTTPS 反向代理或可信 VPN，并使用至少 32 个随机字符的独立令牌。
- 管理员令牌与普通用户令牌不可相同；每人独立令牌，离开项目时禁用或轮换。
- 不要把 `.env`、`lite_users.json`、令牌交付 CSV、NapCat/QQ 数据或 AstrBot 配置提交到 Git。
- ComfyUI、AstrBot、NapCat 与自定义节点都按最小权限运行，定期备份后再升级。
- NSFW 分级是路由辅助而非内容安全保证；公开服务应增加人工审核和平台规则。

## 泄漏后的处理

立即停止 Hub，轮换管理员及所有用户令牌，检查 `hub_state/audit.jsonl` 和反向代理访问日志，
撤销暴露的 QQ/API 凭据，并用 GitHub 的历史重写与 secret scanning 处理误提交。
